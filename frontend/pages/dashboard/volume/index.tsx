import PageTitle from "$/components/page_title";
import type { NextPage } from "next";
import React from "react";

const Volumes: NextPage = () => {
  return (
    <PageTitle title="Disks" onRefresh={() => {}}>
      Not implemented
    </PageTitle>
  );
};

export default Volumes;
